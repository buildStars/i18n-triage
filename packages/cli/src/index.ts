#!/usr/bin/env node
// CLI 入口。Day 1 仅占位；Day 6 实现文件发现、配置加载与报告输出。
// 分层原则：所有 IO（fs / glob / process）只允许出现在这个包里。

const VERSION = '0.0.0'

process.stdout.write(`i18n-triage v${VERSION} — CLI not implemented yet\n`)
