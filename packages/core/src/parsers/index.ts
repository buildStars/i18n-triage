// 解析器层：源码字符串 → StringNode[]
// - vue-sfc.ts   .vue 的 <template> 块（@vue/compiler-sfc + @vue/compiler-dom）
// - script.ts    .ts / .js 以及 .vue 的 <script> 块（ts-morph）
// 全部为纯函数，入参是源码字符串而不是文件路径。

export {}
